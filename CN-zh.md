# B站屏蔽助手 · BiliTitleFilter

> B站视频标题屏蔽助手 —— 一个按标题屏蔽词过滤 B 站视频的浏览器扩展。

![version](https://img.shields.io/badge/version-1.0.0--beta.1-orange)
![manifest](https://img.shields.io/badge/Manifest-V3-blue)
![browser](https://img.shields.io/badge/Edge%20%7C%20Chrome-Chromium-00aeec)
![license](https://img.shields.io/badge/license-未指定-lightgrey)

一个适用于 **Microsoft Edge / Chrome 等 Chromium 浏览器** 的 B 站网页端扩展（Manifest V3）。
按你自己添加的**视频标题屏蔽词**屏蔽视频，并提供两种屏蔽方式、分区推广屏蔽、页面内快捷面板与深色模式适配。

---

## ⚠️ Beta 说明

当前为 **v1.0.0-beta.1**，功能已完整可用，但仍处于测试阶段，欢迎反馈问题。

- 已验证：核心屏蔽逻辑 38 项、设置界面 29 项、后台服务 18 项，共 **85 项自动化校验全部通过**。
- 已知限制：B 站页面结构改版后，个别卡片的识别选择器可能需要更新（见文末「常见问题」）。
- 反馈渠道：请在 [Issues](https://github.com/ke25019/bili-title-filter/issues) 中提交，附上页面地址与截图更好定位。

---

## 功能一览

### 1. 标题屏蔽词屏蔽视频
- 支持批量添加 / 删除屏蔽词，多个屏蔽词之间是「或」的关系。
- 匹配规则可选：**区分大小写**、**正则表达式**、**同时匹配 UP 主名称**。
- 改动立即生效，所有已打开的 B 站页面自动重新扫描。

### 2. 两种屏蔽方式
| 方式 | 效果 |
| --- | --- |
| **整体遮蔽** | 把**封面 + 标题合成一整块区域**，显示「根据您的屏蔽词已将此视频屏蔽」，页面布局保持不变，不会抖动。 |
| **完全隐藏** | 直接把视频卡片从页面上移除（`display:none`），**就像这个视频没有出现过一样**。 |

- 遮蔽文案可自定义（设置页 → 屏蔽方式 → 遮蔽区域文案）。
- 可选「点击遮蔽区域可临时查看」：点一下就能看这个视频，左上角角标可一键恢复屏蔽。

### 3. 屏蔽指定分区推广
可单独开关以下类型的推广卡片：

直播 · 番剧 · 影视 · 课堂/课程 · 专栏/图文 · 动态 · 漫画 · 活动话题推广位 · 商业广告

> 识别方式同时基于**卡片内链接域名/路径**与**卡片 class**，因此对 B 站改版有一定容错性。

### 4. 两处调节入口
- **浏览器扩展页面**：点击工具栏图标弹出快捷面板，或进入「完整设置」页（含效果预览、导入导出、统计等）。
- **B 站页面内**：标题栏附近会注入一个「屏蔽助手」悬浮按钮，点开后可直接切换屏蔽方式、增删屏蔽词、勾选分区、切换外观主题。

### 5. 深色模式适配
- 遮蔽区域与页面内面板会自动跟随 B 站 web 端的深色模式。
- 检测方式：`<html>` 的 `dark` 属性 / `data-theme` / class，并以上下文背景色亮度作为兜底判断。
- 也可手动指定「始终浅色 / 始终深色」（设置页 → 外观与交互）。

### 6. 其它
- 工具栏徽标显示今日屏蔽数量，累计/今日统计可在设置页查看与重置。
- 配置支持**导出 / 导入 JSON**，方便备份与多机同步。
- 设置存放在 `chrome.storage.sync`，登录浏览器账号时可跨设备同步。
- 注入的 UI 全部使用 **Shadow DOM + `bf-` 前缀类名**，不会污染或破坏 B 站自身样式。

---

## 安装方法（Microsoft Edge）

1. 打开 Edge，地址栏输入 `edge://extensions/` 回车。
2. 打开左下角的 **「开发人员模式」** 开关。
3. 点击 **「加载解压缩的扩展」**。
4. 选择本文件夹（即包含 `manifest.json` 的 `bilibili-blocker` 目录）。
5. 打开任意 B 站页面（如 `https://www.bilibili.com`），标题栏附近会出现「屏蔽助手」按钮。

> Chrome 用户同理，访问 `chrome://extensions/` → 开发者模式 → 加载已解压的扩展程序。

---

## 使用说明

1. 点击浏览器工具栏的扩展图标，或点击 B 站页面标题栏附近的「屏蔽助手」按钮。
2. 在「标题屏蔽词」输入框中输入你想屏蔽的关键词（回车添加）。例如：
   - `剧透`
   - `营销号`
   - `标题党`
   - 开启正则后可使用 `^\s*【.*?】`、`(合集|盘点)\s*第?\d+` 等高级规则。
3. 在「屏蔽方式」中二选一：**整体遮蔽** 或 **完全隐藏**。
4. 需要过滤分区推广时，在「屏蔽分区推广」中勾选 直播 / 番剧 等类型。
5. 其余高级选项（正则、UP 主匹配、文案自定义、导入导出、统计）在 **完整设置** 页中调整。

### 支持的页面
首页推荐、搜索页、分区页、排行榜、播放页右侧相关推荐、动态页等常见视频列表页。

---

## 目录结构

```
bilibili-blocker/
├── manifest.json          # MV3 清单
├── background.js          # Service Worker：统计、徽标、默认值、打开设置页
├── shared/
│   └── defaults.js        # 共享默认配置 + 分区类型定义（含识别规则）
├── content/
│   ├── content.js         # 内容脚本：扫描/匹配/屏蔽 + 页面内悬浮面板
│   └── content.css        # 注入样式：遮蔽区域、深色模式变量
├── popup/                 # 工具栏弹窗（快捷设置）
│   ├── popup.html / popup.css / popup.js
├── options/               # 完整设置页
│   ├── options.html / options.css / options.js
└── icons/                 # 16 / 32 / 48 / 128 图标
```

---

## 开发与自测

扩展本体零依赖，纯原生 JS（MV3 + Shadow DOM）。

```bash
cd tests
npm install      # 仅测试需要 jsdom
npm test         # 85 项行为校验
```

验证覆盖：屏蔽逻辑与两种屏蔽方式、分区识别、面板挂载与样式隔离、深色模式、
弹窗 / 设置页交互、后台统计与徽标。详见 `tests/README.md`。

---

## 常见问题

**Q：屏蔽后页面出现空白 / 排版错乱？**
A：说明某个卡片的识别选择器需要更新（B 站改版所致）。可先切换为「整体遮蔽」模式，或到 `content/content.js` 的 `CARD_SELECTOR` / `TITLE_SELECTORS` 中补充新的类名。

**Q：为什么有的视频没被屏蔽？**
A：确认关键词是否被标题包含；若开启了「区分大小写」或「正则表达式」，匹配会更严格。也可打开设置页的「输出调试日志」，在 B 站页面的控制台查看命中情况。

**Q：会影响页面性能吗？**
A：扫描使用 `MutationObserver` + 180ms 防抖，并只对「新增/被改动」的卡片做增量处理，已处理过的卡片不会重复计算。

**Q：统计数字比实际看到的屏蔽数量多？**
A：B 站是单页应用，列表滚动与切换会让卡片重新渲染，统计为「屏蔽次数」的近似值，仅作参考。

---

## 版本

**v1.0.0-beta.1** · 适用于 Microsoft Edge (Chromium) 102+（`minimum_chrome_version: 102`）

### 更新日志

**v1.0.0-beta.1**（首个公开测试版）

- 按标题屏蔽词屏蔽视频，支持批量管理、正则、区分大小写、UP 主名称匹配
- 两种屏蔽方式：整体遮蔽（封面 + 标题合并为一块提示区域）/ 完全隐藏
- 9 类分区推广屏蔽：直播、番剧、影视、课堂、专栏、动态、漫画、活动推广位、商业广告
- B 站页面标题栏附近悬浮快捷面板（Shadow DOM 隔离样式）+ 扩展弹窗 + 完整设置页
- 自动适配 B 站 web 端深色模式，也可手动锁定浅色 / 深色
- 工具栏徽标统计今日屏蔽数，支持配置导入导出

### 路线图

- [ ] 按 UP 主 / 播放量 / 时长 / 分区细分屏蔽
- [ ] 白名单与「始终显示」规则
- [ ] 屏蔽词分组与配置方案（Profile）切换
- [ ] 补齐完整的分区 / 标签屏蔽

